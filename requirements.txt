# Runtime dependencies (mirrors [project].dependencies in pyproject.toml).
# On Termux: pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.34
pillow>=11.0
pyyaml>=6.0
pydantic>=2.0
httpx>=0.28
dirigera>=1.1
defusedxml>=0.7
